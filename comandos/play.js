// commands/play.js
const { MessageEmbed } = require('discord.js');

module.exports = {
  name: 'play',
  aliases: ['shuffle, skip, stop, leave'],
  inVoiceChannel: true,
  async execute(client, message, args) {
    
    }
  }


