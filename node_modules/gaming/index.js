function game() {
    var x = Math.floor(Math.random() * 7);
    console.log(x)
}
function generateRandomInteger(min, max) {
    return Math.floor(min + Math.random()*(max + 1 - min))
  }
function randomNut(y) {
    var y = Math.floor(Math.random() * 4);
    var x = Math.floor(Math.random() * 7);
    var z = [];
    for(i=0;  i < y; i++){
       //var = z[i]+1;
    }

    return Math.floor(min + Math.random()*(max + 1 - min))
  }

game();